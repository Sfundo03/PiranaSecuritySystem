<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PIRANA GUARDING - Secure Access Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-blue: #0069aa;
            --secondary-blue: #004a7c;
            --accent-orange: #f26522;
            --light-bg: #f8f9fa;
            --dark-text: #333333;
            --light-text: #6c757d;
            --border-color: #dee2e6;
        }

        body {
            background-color: var(--light-bg);
            font-family: 'Roboto', sans-serif;
            color: var(--dark-text);
            margin: 0;
            padding: 0;
        }

        /* Top navbar */
        .navbar {
            background-color: var(--primary-blue);
            padding: 15px 30px;
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 22px;
            color: white !important;
        }

        .navbar span {
            font-size: 14px;
            color: #e0e0e0;
        }

        /* Center the login section */
        .login-section {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: calc(100vh - 70px);
            padding: 20px;
        }

        .login-container {
            max-width: 460px;
            width: 100%;
        }

        .login-card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 25px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            border: 1px solid var(--border-color);
        }

        .card-header {
            background: var(--primary-blue);
            color: white;
            text-align: center;
            padding: 25px 20px;
        }

        .card-body {
            padding: 30px;
        }

        .subtitle {
            font-size: 14px;
            color: var(--light-text);
        }

        .form-control {
            padding: 12px 15px;
            border-radius: 6px;
            border: 1px solid var(--border-color);
            font-size: 15px;
        }

            .form-control:focus {
                border-color: var(--primary-blue);
                box-shadow: 0 0 0 0.2rem rgba(0, 105, 170, 0.15);
            }

        .input-icon {
            position: relative;
        }

            .input-icon i {
                position: absolute;
                left: 15px;
                top: 50%;
                transform: translateY(-50%);
                color: var(--light-text);
            }

            .input-icon input {
                padding-left: 45px;
            }

        .btn-login {
            background: var(--primary-blue);
            border: none;
            padding: 12px;
            border-radius: 6px;
            font-weight: 500;
            transition: all 0.3s;
            font-size: 16px;
        }

            .btn-login:hover {
                background: var(--secondary-blue);
            }

        .btn-register {
            border: 1px solid var(--primary-blue);
            color: var(--primary-blue);
            padding: 10px 20px;
            border-radius: 6px;
            font-weight: 500;
            transition: all 0.3s;
            font-size: 14px;
        }

            .btn-register:hover {
                background: var(--primary-blue);
                color: white;
            }

        .divider {
            display: flex;
            align-items: center;
            margin: 20px 0;
        }

            .divider::before, .divider::after {
                content: "";
                flex: 1;
                border-bottom: 1px solid var(--border-color);
            }

            .divider span {
                padding: 0 10px;
                color: var(--light-text);
                font-size: 13px;
            }

        .password-toggle {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: var(--light-text);
        }

        .company-info {
            margin-top: 40px;
            text-align: center;
            color: var(--light-text);
        }

            .company-info h5 {
                font-weight: 600;
                color: var(--primary-blue);
            }

            .company-info p {
                font-size: 14px;
                max-width: 600px;
                margin: 0 auto;
            }
    </style>
</head>
<body>
   

    <!-- Login Section -->
    <div class="login-section">
        <div class="login-container">
            <div class="login-card">
                <div class="card-header">
                    <h4 class="mb-0">System Login</h4>
                    <small>Enter your credentials to access the system</small>
                </div>
                <div class="card-body">
                    @if (ViewBag.ErrorMessage != null)
                    {
                        <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
                    }

                    @using (Html.BeginForm("Login", "Account", FormMethod.Post, new { @id = "loginForm" }))
                    {
                        @Html.AntiForgeryToken()

                        <div class="mb-4">
                            <div class="input-icon">
                                <i class="fas fa-envelope"></i>
                                <input type="email" class="form-control" id="Email" name="Email" placeholder="Email address" required>
                            </div>
                        </div>

                        <div class="mb-3">
                            <div class="input-icon">
                                <i class="fas fa-lock"></i>
                                <input type="password" class="form-control" id="Password" name="Password" placeholder="Password" required>
                                <span class="password-toggle" id="passwordToggle">
                                    <i class="fas fa-eye"></i>
                                </span>
                            </div>
                        </div>

                        <div class="mb-3 form-check">
                            <input type="checkbox" class="form-check-input" id="RememberMe" name="RememberMe">
                            <label class="form-check-label" for="RememberMe">Remember me</label>
                        </div>

                        <button type="submit" class="btn btn-login w-100 text-white">
                            <i class="fas fa-sign-in-alt me-2"></i>Log In
                        </button>

                        <div class="text-center mt-3">
                            <a href="/Account/ForgotPassword">Forgot your password?</a>
                        </div>
                    }

                    <div class="divider">
                        <span>OR</span>
                    </div>

                    <div class="text-center">
                        <a href="/Resident/Register" class="btn btn-register">Create New Account</a>
                    </div>
                </div>
            </div>

            <!-- Company Info -->
            <div class="company-info">
                <h5>About Pirana Guarding</h5>
                <p>
                    Pirana Guarding is a leading security company based in Johannesburg,
                    specializing in residential, commercial, and corporate protection.
                    With years of experience, we provide reliable guarding services,
                    advanced access management, and 24/7 monitoring to keep our clients safe.
                    Our trained security personnel and innovative technology solutions
                    make us a trusted partner for businesses and communities across Joburg.
                </p>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const passwordToggle = document.getElementById('passwordToggle');
            const passwordInput = document.getElementById('Password');

            // Toggle password visibility
            passwordToggle.addEventListener('click', function () {
                const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
                passwordInput.setAttribute('type', type);
                this.querySelector('i').classList.toggle('fa-eye');
                this.querySelector('i').classList.toggle('fa-eye-slash');
            });
        });
    </script>
</body>
</html>
